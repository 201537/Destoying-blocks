#include <stdio.h>

int main()
{
    int n,i,temp,rem,sum=0;
    scanf("%d",&n);
    temp=n;
    while(n!=0)
{
    rem=n%10;
    sum=sum+rem;
    n=n/10;
}

printf("The sum of the digits in %d : %d",temp,sum);
    
    }
